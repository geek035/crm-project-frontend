import { HasControlErrorsPipe } from './has-control-errors-pipe';

describe('HasControlErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new HasControlErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
