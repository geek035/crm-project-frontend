import { InfoBlockFullnamePipe } from './info-block-fullname-pipe';

describe('InfoBlockFullnamePipe', () => {
  it('create an instance', () => {
    const pipe = new InfoBlockFullnamePipe();
    expect(pipe).toBeTruthy();
  });
});
