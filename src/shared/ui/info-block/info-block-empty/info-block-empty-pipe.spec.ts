import { InfoBlockEmptyPipe } from './info-block-empty-pipe';

describe('InfoBlockEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new InfoBlockEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
